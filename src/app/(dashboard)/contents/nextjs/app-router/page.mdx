
## この記事で学ぶこと

- App Router とは
- app ディレクトリ
- page.jsx の役割
- layout.jsx の役割

---

## App Router とは

**App Router** は Next.js 13 で導入された新しいルーティングシステムです。

```
特徴：
├── Server Components がデフォルト
├── レイアウトの入れ子が可能
├── loading.jsx, error.jsx が使える
├── より直感的なルート定義
└── React 18 の機能をフル活用
```

---

## app ディレクトリ

app ディレクトリ内のファイル構造が、そのまま URL になります。

```
app/
├── page.jsx          → /
├── about/
│   └── page.jsx      → /about
├── blog/
│   └── page.jsx      → /blog
└── contact/
    └── page.jsx      → /contact
```

---

## page.jsx の役割

**page.jsx** はそのルートに対応するページコンポーネントです。

```jsx
// app/page.jsx（トップページ）
export default function Home() {
  return (
    <div>
      <h1>ホームページ</h1>
    </div>
  );
}
```

```jsx
// app/about/page.jsx（/about ページ）
export default function About() {
  return (
    <div>
      <h1>About ページ</h1>
      <p>このサイトについて</p>
    </div>
  );
}
```

---

## layout.jsx の役割

**layout.jsx** は複数のページで共有されるUIを定義します。

```jsx
// app/layout.jsx（ルートレイアウト）
import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>
        <header className="bg-blue-600 text-white p-4">
          <h1>My Site</h1>
        </header>
        <main>{children}</main>
        <footer className="bg-gray-200 p-4 mt-8">
          &copy; 2024 My Site
        </footer>
      </body>
    </html>
  );
}
```

### children とは

`{children}` には、各ページの内容（page.jsx）が入ります。

```
layout.jsx
├── <header>
├── {children}  ← ここにページが入る
└── <footer>
```

---

## 実践：About ページを作成

### 1. フォルダとファイルを作成

```bash
mkdir app/about
touch app/about/page.jsx
```

### 2. ページを作成

```jsx
// app/about/page.jsx
export default function About() {
  return (
    <div className="p-8">
      <h1 className="text-3xl font-bold">About</h1>
      <p className="mt-4">
        このサイトは Next.js で作成されています。
      </p>
    </div>
  );
}
```

### 3. 確認

http://localhost:3000/about にアクセスして確認します。

---

## 🎯 Try: さらにページを追加

同様の手順で `/contact` ページを作成してみましょう。

1. `app/contact/page.jsx` を作成
2. お問い合わせページの内容を表示
3. http://localhost:3000/contact で確認

---

## まとめ

```
App Router の基本：
├── app/ 内のフォルダがルートになる
├── page.jsx がそのルートのページ
├── layout.jsx が共通UIを定義
└── {children} にページ内容が入る
```

次の記事では、動的ルーティングを学びます。

---

## 次の記事へ

07-04. ファイルベースルーティング
