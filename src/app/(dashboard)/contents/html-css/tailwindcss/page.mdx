# Tailwind CSS 入門

> 📌 **この記事で学ぶこと**
>
> - Tailwind CSS とは何か
> - React プロジェクトへの導入
> - 基本的なクラス名
> - Tailwind の書き方

## Tailwind CSS とは？

**Tailwind CSS** は、ユーティリティファーストの CSS フレームワークです。

従来の CSS：

```css
.button {
  background-color: blue;
  color: white;
  padding: 10px 20px;
  border-radius: 8px;
}
```

Tailwind CSS：

```html
<button class="bg-blue-500 text-white px-5 py-2 rounded-lg">ボタン</button>
```

**クラス名を組み合わせる**だけでスタイリングができます！

<!--
📸 画像メモ: 従来のCSSとTailwindの比較
- 左: CSS ファイルと HTML ファイル
- 右: Tailwind だけで完結
-->

## Tailwind のメリット

1. **CSS ファイルを書く必要がない**
2. **クラス名が直感的**（`bg-blue-500` = 背景青）
3. **レスポンシブ対応が簡単**（`md:flex`）
4. **デザインの統一性**（色やサイズが決まっている）

## React + Vite への導入

### 1. プロジェクトを作成（または既存のプロジェクトで）

```bash
cd ~/Desktop/webex
npm create vite@latest tailwind-practice -- --template react
cd tailwind-practice
npm install
```

### 2. Tailwind CSS をインストール

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### 3. tailwind.config.js を編集

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

### 4. src/index.css を編集

既存の内容を削除して、以下を追加：

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### 5. 動作確認

`npm run dev` で開発サーバーを起動し、`src/App.jsx` を編集：

```jsx
function App() {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <h1 className="text-4xl font-bold text-blue-600">Hello Tailwind!</h1>
    </div>
  );
}

export default App;
```

青い文字が表示されれば成功！

<!--
📸 画像メモ: Hello Tailwind! の表示
-->

## 基本的なクラス名

### レイアウト

| クラス   | 説明           |
| -------- | -------------- |
| `flex`   | display: flex  |
| `grid`   | display: grid  |
| `block`  | display: block |
| `hidden` | display: none  |

### Flexbox

| クラス            | 説明                           |
| ----------------- | ------------------------------ |
| `justify-center`  | justify-content: center        |
| `justify-between` | justify-content: space-between |
| `items-center`    | align-items: center            |
| `flex-col`        | flex-direction: column         |
| `gap-4`           | gap: 1rem (16px)               |

### サイズ

| クラス     | 説明                 |
| ---------- | -------------------- |
| `w-full`   | width: 100%          |
| `w-1/2`    | width: 50%           |
| `w-64`     | width: 16rem (256px) |
| `h-screen` | height: 100vh        |
| `max-w-xl` | max-width: 36rem     |

### スペーシング（Margin / Padding）

| クラス    | 説明                       |
| --------- | -------------------------- |
| `m-4`     | margin: 1rem               |
| `mx-auto` | margin-left/right: auto    |
| `mt-8`    | margin-top: 2rem           |
| `p-4`     | padding: 1rem              |
| `px-6`    | padding-left/right: 1.5rem |
| `py-2`    | padding-top/bottom: 0.5rem |

> 💡 **数値のルール**
>
> - 1 = 0.25rem (4px)
> - 2 = 0.5rem (8px)
> - 4 = 1rem (16px)
> - 8 = 2rem (32px)

### 色

| クラス          | 説明             |
| --------------- | ---------------- |
| `bg-white`      | 背景: 白         |
| `bg-gray-100`   | 背景: 薄いグレー |
| `bg-blue-500`   | 背景: 青         |
| `text-white`    | 文字: 白         |
| `text-gray-700` | 文字: 濃いグレー |

> 💡 **色の数値**
>
> - 50 〜 950 まで（50 が最も薄い）
> - 500 が標準的な色

### テキスト

| クラス        | 説明       |
| ------------- | ---------- |
| `text-sm`     | 小さい     |
| `text-base`   | 標準       |
| `text-lg`     | 大きい     |
| `text-2xl`    | 2 倍大きい |
| `font-bold`   | 太字       |
| `text-center` | 中央揃え   |

### ボーダー

| クラス            | 説明             |
| ----------------- | ---------------- |
| `border`          | ボーダーあり     |
| `border-2`        | 2px のボーダー   |
| `border-gray-300` | グレーのボーダー |
| `rounded`         | 少し角丸         |
| `rounded-lg`      | 大きめの角丸     |
| `rounded-full`    | 完全な丸         |

### シャドウ

| クラス      | 説明       |
| ----------- | ---------- |
| `shadow`    | 軽い影     |
| `shadow-md` | 中程度の影 |
| `shadow-lg` | 大きな影   |

## レスポンシブ

画面サイズに応じたスタイルは、プレフィックスを付けるだけ！

```html
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3">
  <!-- モバイル: 1列, タブレット: 2列, PC: 3列 -->
</div>
```

| プレフィックス | 画面幅      |
| -------------- | ----------- |
| なし           | 全サイズ    |
| `sm:`          | 640px 以上  |
| `md:`          | 768px 以上  |
| `lg:`          | 1024px 以上 |
| `xl:`          | 1280px 以上 |

## ホバー・フォーカス

```html
<button class="bg-blue-500 hover:bg-blue-600 focus:ring-2">ボタン</button>
```

## 実践例：カードコンポーネント

```jsx
function Card({ title, description }) {
  return (
    <div className="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
      <h3 className="text-xl font-bold text-gray-800 mb-2">{title}</h3>
      <p className="text-gray-600">{description}</p>
    </div>
  );
}
```

## 実践例：ボタン

```jsx
function Button({ children }) {
  return (
    <button className="bg-blue-500 text-white px-6 py-2 rounded-lg font-medium hover:bg-blue-600 transition-colors">
      {children}
    </button>
  );
}
```

## 便利なリソース

- [Tailwind CSS 公式ドキュメント](https://tailwindcss.com/docs)
- [Tailwind CSS Cheat Sheet](https://nerdcave.com/tailwind-cheat-sheet)

## まとめ

- **Tailwind CSS** はユーティリティクラスを組み合わせてスタイリング
- **クラス名が直感的**で覚えやすい
- **レスポンシブ**は `md:` などのプレフィックス
- **ホバー**は `hover:` プレフィックス
- React との相性が抜群！

---

## 🔗 次のステップ

Tailwind の基礎がわかったら、[まとめと次のステップ](./01-10_まとめと次のステップ.md) で HTML/CSS 章を振り返りましょう！
